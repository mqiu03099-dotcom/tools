@import "../../utils/unlock-dialog.wxss";

.avatar-page {
  min-height: 100vh;
  width: 100%;
  background: #000000;
}

.avatar-page-body {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
  padding: 0;
  width: 100%;
  box-sizing: border-box;
}

.avatar-page-intro {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
  padding: 20rpx;
  border-radius: 0;
  background:
    radial-gradient(circle at top left, rgba(255, 255, 255, 0.92), transparent 36%),
    linear-gradient(180deg, #f8f9fb 0%, #f1f4f7 100%);
  box-shadow: inset 0 2rpx 0 rgba(255, 255, 255, 0.72);
}

.avatar-page-intro-title {
  display: block;
  color: #111111;
  font-size: 40rpx;
  font-weight: 700;
  line-height: 1.3;
  letter-spacing: 0.8rpx;
}

.avatar-page-intro-text {
  display: block;
  color: rgba(17, 17, 17, 0.62);
  font-size: 26rpx;
  line-height: 1.7;
}

.avatar-page-intro-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 20rpx;
}

.avatar-page-intro-tag {
  padding: 12rpx 20rpx;
  border-radius: 1998rpx;
  color: rgba(17, 17, 17, 0.72);
  font-size: 22rpx;
  line-height: 1;
  background: rgba(255, 255, 255, 0.82);
  border: 2rpx solid rgba(17, 17, 17, 0.05);
  box-sizing: border-box;
}

.avatar-page-intro-tag-primary {
  color: #2f4f76;
  font-weight: 700;
  letter-spacing: 2rpx;
  background: linear-gradient(180deg, rgba(228, 238, 248, 0.98), rgba(211, 226, 242, 0.98));
  border-color: rgba(74, 112, 158, 0.28);
  box-shadow: inset 0 2rpx 0 rgba(255, 255, 255, 0.72);
}

.avatar-loading {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 20rpx;
  padding: 36rpx 0 12rpx;
}

.avatar-loading-image-wrap {
  width: 36rpx;
  height: 36rpx;
  border-radius: 1998rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.avatar-loading-image {
  width: 100%;
  height: 100%;
  display: block;
  filter: brightness(0) invert(1);
  animation: avatarLoadingRotate 0.9s linear infinite;
}

@keyframes avatarLoadingRotate {
  from {
    transform: rotate(0deg);
  }

  to {
    transform: rotate(360deg);
  }
}

.avatar-loading-text {
  color: rgba(17, 17, 17, 0.46);
  font-size: 20rpx;
  line-height: 1;
}

.avatar-group-card {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
  padding: 20rpx;
  border-radius: 0;
  background:
    radial-gradient(circle at top left, rgba(255, 255, 255, 0.92), transparent 36%),
    linear-gradient(180deg, #f8f9fb 0%, #f1f4f7 100%);
  box-sizing: border-box;
}

.avatar-group-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 20rpx;
}

.avatar-group-title {
  flex: 1;
  color: #111111;
  font-size: 32rpx;
  font-weight: 700;
  line-height: 1.3;
}

.avatar-group-action {
  min-width: 0;
  margin: 0 !important;
  padding: 12rpx 20rpx;
  height: auto;
  line-height: 1;
  border: 0;
  border-radius: 1998rpx;
  display: inline-flex;
  align-items: center;
  gap: 8rpx;
  color: #2f4f76;
  font-size: 22rpx;
  font-weight: 700;
  letter-spacing: 2rpx;
  width: max-content;
  max-width: 100%;
  background: linear-gradient(180deg, rgba(228, 238, 248, 0.98), rgba(211, 226, 242, 0.98));
  border: 2rpx solid rgba(74, 112, 158, 0.28);
  box-shadow: inset 0 2rpx 0 rgba(255, 255, 255, 0.72);
  box-sizing: border-box;
}

.avatar-group-action-text {
  line-height: 1;
}

.avatar-group-scroll {
  width: 100%;
}

.avatar-group-thumb-list {
  display: grid;
  grid-template-columns: repeat(5, minmax(0, 1fr));
  gap: 20rpx;
}

.avatar-image-box {
  position: relative;
  width: 100%;
  aspect-ratio: 1 / 1;
  border-radius: 16rpx;
  overflow: hidden;
  background: linear-gradient(180deg, #edf1f5 0%, #e3e8ee 100%);
  box-shadow: inset 0 2rpx 0 rgba(255, 255, 255, 0.78);
}

.avatar-image-box-image {
  width: 100%;
  height: 100%;
  display: block;
}

.avatar-preview-dialog {
  padding-top: 36rpx;
}

.avatar-preview-shell {
  max-width: 664rpx;
}

.avatar-preview-image-wrap {
  margin: 28rpx 0 20rpx;
  aspect-ratio: 1 / 1;
  border-radius: 16rpx;
  background: linear-gradient(180deg, #f3f5f8 0%, #e8edf2 100%);
  overflow: hidden;
  display: flex;
  align-items: center;
  justify-content: center;
}

.avatar-preview-image {
  width: auto;
  height: 100%;
  display: block;
}

.avatar-preview-group-scroll {
  width: 100%;
  max-height: 472rpx;
  margin: 28rpx 0 20rpx;
}

.avatar-preview-group-grid {
  display: grid;
  grid-template-columns: repeat(5, minmax(0, 1fr));
  gap: 16rpx;
}

.avatar-preview-group-item {
  aspect-ratio: 1 / 1;
  border-radius: 16rpx;
  overflow: hidden;
  background: #1a1a1a;
}

.avatar-preview-group-image {
  width: 100%;
  height: 100%;
  display: block;
}

.avatar-page {
  background: #000000;
  color: #ffffff;
}

.avatar-page-intro-title,
.avatar-group-title {
  color: #ffffff;
}

.avatar-page-intro,
.avatar-group-card {
  border-radius: 0;
  background: #111111;
  box-shadow: none;
}

.avatar-page-intro-text {
  color: rgba(255, 255, 255, 0.62);
}

.avatar-page-intro-tag {
  color: rgba(255, 255, 255, 0.72);
  background: rgba(255, 255, 255, 0.08);
  border-color: rgba(255, 255, 255, 0.1);
}

.avatar-page-intro-tag-primary,
.avatar-group-action {
  color: #d3f3ea;
  background: rgba(114, 176, 160, 0.18);
  border-color: rgba(114, 176, 160, 0.28);
  box-shadow: none;
}

.avatar-loading-text {
  color: rgba(255, 255, 255, 0.48);
}

.avatar-image-box,
.avatar-preview-image-wrap {
  border-radius: 16rpx;
  background: #1a1a1a;
  box-shadow: none;
}
