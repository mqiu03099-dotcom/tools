export function getAssetTitleOptions(jsonText: string): string[];

export function appendRemoteUrlToTitle(params: {
  jsonText: string;
  selectedTitle: string;
  remoteUrl: string;
}): string;
